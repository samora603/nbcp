# Infrastructure

Infrastructure-as-code and local topology for NBCP.

| Path | Purpose |
| --- | --- |
| `docker/` | Local Compose / container definitions (future) |
| `terraform/` | Cloud infrastructure modules (future) |

Hosting decisions for application compute will be captured in a future ADR. PostgreSQL hosting via **Supabase** is accepted in [ADR-0001](../docs/adr/0001-platform-technology-foundation.md).

**Phase 0.1:** placeholders only — no Compose or Terraform resources yet.
