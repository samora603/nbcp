# Generators

Code generators (e.g., Plop/Hygen) will enforce the modular hexagonal template for new modules and packages.

**Phase 0.1:** not implemented. Do not manually invent divergent module layouts in later phases — introduce a generator first.
