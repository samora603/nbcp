# Packages

Shared technical libraries will live here (config presets, UI design system, contracts, telemetry, auth SDK, etc.).

**Phase 0.1:** intentionally empty of package source — workspace globs are reserved in `pnpm-workspace.yaml`.
