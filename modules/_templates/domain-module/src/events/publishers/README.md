# Outbox / bus publisher adapters

Implement `EventPublisher` in `infrastructure/messaging/`.
Keep this folder for module-local publish helpers if needed; prefer the port.

See `docs/architecture/module-standard.md` and `docs/architecture/eventing.md`.
